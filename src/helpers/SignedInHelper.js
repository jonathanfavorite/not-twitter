import react from "react";
import { useContext, useEffect, useState } from "react";
import { SignedInUserContext } from "../contexts/SignedInUserDetailsContext";

function SignedInHelper() {
    // const signedInContext = useContext(SignedInUserContext);


    // if(!signedInContext.IsCached)
    // {
    //     console.log("Storage Token: " + localStorage.getItem("token"));
    //     console.log("Context User: " + JSON.stringify(signedInContext));
    //     //signedInContext.SetCached(true);
    //     signedInContext.handleSetUserLoaded(false);

    // }
    // signedInContext.SetCached(true);
    // signedInContext.handleSetUserLoaded(true);

    // useEffect(() => {
    //  // checkForTokenInLocalStorage();
    //  signedInContext.SetUser(null);
    // }, []);

}


